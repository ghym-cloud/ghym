pipeline {
  agent any
  triggers {
    githubPush()
  }
  stages {
    stage('Service') {
      steps {
        sh '''
         for f in $(git diff --name-only HEAD HEAD~1 | grep service)
         do

         done
        '''
      }
    }
  }
}
