pipeline {
  agent any
  triggers {
    githubPush()
  }
  stages {
    stage('LoadBalancer') {
      steps {
        echo '* Building started *'
        sh '''
         uname -a
        '''
        echo '* Building finished *'
      }
    }
  }
}
