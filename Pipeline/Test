pipeline {
  agent any
  triggers {
    githubPush()
  }
  stages {
    stage('LoadBalancer') {
      steps {
        echo '* Building started *'
        sh '''
         pwd
        '''
        echo '* Building finished *'
      }
    }
  }
}
