pipeline {
  agent any
  triggers {
    githubPush()
  }
  stages {
    stage('DNS') {
      steps {
        sh '''
         a='          value: "'
         while read -r line
         do
          a="${a}$line,"
         done < DNS
         echo $a
        '''
      }
    }
  }
}
