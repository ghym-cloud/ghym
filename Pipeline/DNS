pipeline {
  agent any
  trigers {
    githubPush()
  }
  stages {
    stage('DNS') {
      step {
        sh '''
         a='          value: "'
         while read -r line
         do
          a="${a}$line,"
         done < DNS
         echo $a
        '''
      }
    }
  }
}
