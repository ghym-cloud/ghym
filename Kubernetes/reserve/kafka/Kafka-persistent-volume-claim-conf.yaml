apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-conf
  namespace: ghym
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
