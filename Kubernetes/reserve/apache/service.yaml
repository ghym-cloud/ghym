apiVersion: v1
kind: Service
metadata:
  name: apache
  namespace: ghym
spec:
  selector:
    app: apache
  ports:
  - name: http
    protocol: TCP
    port: 80
  - name: https
    protocol: TCP
    port: 443
