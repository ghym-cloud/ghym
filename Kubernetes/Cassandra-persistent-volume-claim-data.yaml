apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cassandra-data
  namespace: ghym
spec:
   accessModes:
     - ReadWriteOnce
   storageClassName: local-storage
   resources:
       requests:
           storage: 10Gi
