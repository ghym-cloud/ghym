auth:
  password: "red1$"
podSecurityContext:
  enabled: false
containerSecurityContext:
  runAsUser: 0
  runAsNonRoot: false
master:
  persistence:
    storageClass: local-storage
replica:
  replicaCount: 1
  persistence:
    storageClass: local-storage
